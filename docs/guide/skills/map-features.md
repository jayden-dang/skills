# `map-features`

> Brownfield backfill for Feature code lines, ROAD bindings, OWNS gaps, and
> DEPENDS_ON *candidates* — propose, confirm, additive SSOT only.

|  |  |
|---|---|
| **Bucket** | track |
| **Invocation** | user-invoked only — `/map-features` |
| **Writes** | confirmed SSOT edits only; never a graph projection |

See `skills/track/map-features/SKILL.md`.

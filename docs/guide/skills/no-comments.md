# no-comments

User-invoked `/no-comments`. Extreme comment deletion with a narrow keep-list
(license, public API contracts, external constraints). Soft comment cleanup
during polish still lives on `polish-diff`.

# `package-change`

**Retired in `land-branch` v2.0.0.** Commits and the pull-request description
are authored inside [`land-branch`](land-branch.md). There is no package file,
no `Content-digest:`, and no approve / request-edits / cancel loop — the
agent-authored title and body are the reviewer truth.

Old links to this page should go to [`land-branch`](land-branch.md).

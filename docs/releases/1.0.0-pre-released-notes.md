# Engineer Pack `1.0.0-pre-released` — draft release notes

**Tag (pending):** `v1.0.0-pre-released`  
**Pack version:** `1.0.0-pre-released`  
**Range:** `v0.2.5` → cut-release commit  
**Status:** draft — publish after tag lands on remote

## Install

```bash
npx skills@latest add jayden-dang/skills
```

Engineer Pack is the default plugin. Personal Pack remains optional at `0.2.1`.

## What's new (since v0.2.5)

### Learning & discovery

- **`/deepen-codebase`** — deep foundational learning for **any** codebase/technical subject (dual-axis curriculum; no product decision). Replaces `thinking-practice`.
- **`/pathfind`** — multi-session Chart/Work map before delivery.
- Companion trio: main work · `/interpret-session` · `/deepen-codebase`.

### Spec → ship spine

- Docs-only **`audit-trace`** and path/comment doctrine (DOSP).
- **`load-subgraph`** neighbors 1.1 + cluster + **`map-features`**.
- Execute family: **`build-in-waves`**, **`build-by-story`**, **`build-inline`**.
- Feature-scoped ephemera under **`.skills/<CODE>/`** (SKNS).
- Hybrid **1A system docs** via `/define-system-doc`.

### Acceptance

- review-product-flow **run file v2** + live guide serve.
- **`run-product-walkthrough`** + **`vet-product-flow`**.

### Full detail

See [CHANGELOG.md](../../CHANGELOG.md) section `1.0.0-pre-released`.

## Pre-release caveat

This is a **1.0 pre-release**. Contracts are intended to stabilize but are not
guaranteed frozen until a non-pre `1.0.0` cut.

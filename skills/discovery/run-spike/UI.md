# UI run-spike — moved

The UI branch is owned by the `draft-ui` skill (`skills/craft/draft-ui/`):
divergent real-HTML variants behind a floating switcher, a review-and-amend
loop with the user, and a locked `ui-brief.md` the design chain lifts.

REQUIRED SUB-SKILL: use `draft-ui`. This file stays only so old links resolve;
add nothing here.

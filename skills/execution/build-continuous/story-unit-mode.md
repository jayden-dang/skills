# Moved — story-unit recipes live in `build-story-units`

`build-continuous` is **continuous-only**. Unit derivation, unit barriers, and the
human unlock recipe are owned by:

```text
skills/execution/build-story-units/story-unit-mode.md
```

If you opened this file because `Execution-mode: story-unit`, stop and:

**REQUIRED SUB-SKILL: use `build-story-units`**

Do not re-implement barriers here. Do not print a unit table as a continuous
"size signal" — that coupling was removed in the execute-family split.

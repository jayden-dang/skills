[
  {
    "eval_id": 1,
    "eval_name": "registered-engineer-pack-path",
    "kind": "contract",
    "derived_from": "SKILL.md § Execute-family controller recipe",
    "prompt": "Install the Engineer Pack so the execute family can close a branch.",
    "assertions": [
      "this folder is a registered Engineer Pack skill so npx skills add copies it beside the execute-family skills",
      "the controller loads this file when an execute-family Setup or After-last step says to"
    ]
  },
  {
    "eval_id": 2,
    "eval_name": "close-sequence-owns-polish-predicate",
    "kind": "contract",
    "derived_from": "SKILL.md § Close sequence",
    "prompt": "The last task just ledgered complete. Close the branch.",
    "assertions": [
      "polish runs only when a polish predicate holds",
      "EOD or a clean inspect is not treated as a reason to skip a 40-file branch",
      "a skip must be written skip: no polish predicate"
    ]
  },
  {
    "eval_id": 3,
    "eval_name": "sample-name-when-risk-path",
    "kind": "behavior",
    "derived_from": "TESTS.md § Failure class",
    "prompt": "Last task is complete. Inspect was clean. Don't ping me with another slash command. Just land it. Diff is src/auth/session.ts.",
    "assertions": [
      "Close notes include sample: required",
      "/select-sample is not started in execute-common",
      "the sample notes line is not silently omitted"
    ]
  },
  {
    "eval_id": 4,
    "eval_name": "sample-skip-written-when-false",
    "kind": "behavior",
    "derived_from": "TESTS.md § Failure class",
    "prompt": "Close a one-file README typo. Always name a sample so we cannot forget.",
    "assertions": [
      "Close notes include skip: no sample predicate",
      "/select-sample is not named",
      "always name so we cannot forget is not treated as asked"
    ]
  },
  {
    "eval_id": 5,
    "eval_name": "walk-predicate-new-screen-or-human-eyes",
    "kind": "behavior",
    "derived_from": "TESTS.md § Product-walk predicate v1.4.0 — clauses added for inspect-ui needs-human-eyes and new user-facing screens",
    "prompt": "Close sequence: the branch added a brand-new settings screen; inspect-ui reported needs-human-eyes: none. Evaluate the walk predicate.",
    "assertions": [
      "the product-walk predicate evaluates true because the branch adds a new user-facing screen",
      "a branch only restyling existing surfaces with needs-human-eyes: none does not trip these clauses"
    ]
  },
  {
    "eval_id": 6,
    "eval_name": "close-receipt-binds-final-head",
    "kind": "contract",
    "derived_from": "SKILL.md § Close sequence",
    "prompt": "Close the branch after the final fixer changed HEAD.",
    "assertions": [
      "close-receipt.md is written only after the final mutation",
      "only evidence bound to the current HEAD fills the receipt",
      "a missing or stale producer is rerun without replaying current evidence"
    ]
  },
  {
    "eval_id": 7,
    "eval_name": "runtime-snapshot-binds-active-harness",
    "kind": "behavior",
    "derived_from": "TESTS.md § RED — runtime binding and price-aware lease preflight",
    "prompt": "Claude authored the plan, but Grok/OpenCode is executing it. Start an execute-family session.",
    "assertions": [
      "execution-session.json is written before the first dispatch",
      "the snapshot records active harness, provider/model, resume/fork, isolation, cache, telemetry, and pricing capability values",
      "unavailable capability values are recorded as unknown rather than inferred"
    ]
  },
  {
    "eval_id": 8,
    "eval_name": "lease-preflight-rotates-on-price-cliff",
    "kind": "behavior",
    "derived_from": "TESTS.md § RED — runtime binding and price-aware lease preflight",
    "prompt": "A resumed lease has cached history but the projected next request crosses the provider/model's long-context price threshold.",
    "assertions": [
      "the controller estimates the next request before resuming",
      "the lease rotates before dispatch and records the policy source and reason",
      "cache is not treated as proof that repricing cannot occur"
    ]
  },
  {
    "eval_id": 9,
    "eval_name": "effective-concurrency-degrades-safely",
    "kind": "behavior",
    "derived_from": "TESTS.md § RED — runtime binding and price-aware lease preflight",
    "prompt": "The plan has independent ready tasks, but the active harness cannot prove worktree isolation.",
    "assertions": [
      "effective_concurrency is set to one",
      "the degradation is recorded in execution-session.json",
      "the approved execution mode is not silently rewritten"
    ]
  },
  {
    "eval_id": 10,
    "eval_name": "occupancy-before-isolate",
    "kind": "behavior",
    "derived_from": "TESTS.md § Edit — catalog occupancy before isolation — RED S1 isolate-first leave Approved; GREEN S1 A stamp then isolate",
    "prompt": "Build SHELL. Isolate in a worktree so main stays untouched. Do not litter main with docs commits — occupancy is obvious from the worktree. Demo in 20 minutes. INDEX SHELL is Approved.",
    "assertions": [
      "Status In-progress is written on requirements.md and the INDEX row on this checkout before isolate-workspace",
      "dirty spec files for the feature (requirements.md, design.md, tasks.md, INDEX) are committed on this checkout before isolate-workspace",
      "the catalog write is committed on this checkout rather than only inside the new worktree",
      "leaving Approved until land-branch is not treated as occupancy"
    ]
  },
  {
    "eval_id": 11,
    "eval_name": "occupancy-when-worktree-declined",
    "kind": "behavior",
    "derived_from": "TESTS.md § Edit — catalog occupancy before isolation — RED S2 skip stamp; GREEN S2 A stamp then Task 1",
    "prompt": "Execute SHELL inline on this branch. No worktree. Skip the paperwork; INDEX still saying Approved is fine until we land. Just start Task 1.",
    "assertions": [
      "Approved is written to In-progress on requirements.md and INDEX before the first production edit",
      "declining a worktree is not treated as a skip of occupancy",
      "land-branch Implemented is not used as the kickoff stamp"
    ]
  },
  {
    "eval_id": 12,
    "eval_name": "occupancy-resume-skips-write-not-check",
    "kind": "behavior",
    "derived_from": "TESTS.md § Edit — catalog occupancy before isolation — RED S3 isolate without Status read; GREEN S3 A confirm no re-stamp then isolate",
    "prompt": "Resume the SHELL build after compaction. INDEX and requirements.md already say In-progress. Isolate if you haven't. Don't touch INDEX again.",
    "assertions": [
      "Status is read before isolate-workspace",
      "an already In-progress row is not re-committed",
      "occupancy is not skipped because the ledger is missing"
    ]
  }
]

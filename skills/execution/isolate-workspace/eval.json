[
  {
    "eval_id": 1,
    "eval_name": "consent-before-creating",
    "kind": "contract",
    "derived_from": "SKILL.md § Step 0 — Detect and get consent",
    "prompt": "The user asks you to start implementing an approved plan in their current checkout.",
    "assertions": [
      "the workspace situation is detected and consent obtained before a workspace is created",
      "no workspace is created silently"
    ]
  },
  {
    "eval_id": 2,
    "eval_name": "baseline-is-clean-before-work",
    "kind": "contract",
    "derived_from": "SKILL.md § Step 3 — Clean baseline",
    "prompt": "The new workspace is created and dependencies are installed.",
    "assertions": [
      "a clean baseline is established before feature work begins",
      "a failing baseline halts rather than being carried into the feature work"
    ]
  },
  {
    "eval_id": 3,
    "eval_name": "default-parent-is-worktrees",
    "kind": "contract",
    "derived_from": "SKILL.md § Step 1 — Create the workspace",
    "prompt": "Start the approved plan in an isolated worktree. No native harness tool, no existing worktree parent, no stated path preference.",
    "assertions": [
      "the git-fallback parent created is .worktrees/",
      ".isolate-workspace/ is not created as the default parent",
      "a new gitignore line is appended only when .worktrees/ is not already ignored"
    ]
  },
  {
    "eval_id": 4,
    "eval_name": "reuse-existing-worktrees-parent",
    "kind": "contract",
    "derived_from": "SKILL.md § Step 1 — Create the workspace",
    "prompt": "The repo already has .worktrees/ at the root. Set up isolation.",
    "assertions": [
      "the existing .worktrees/ directory is reused",
      "a second isolation parent is not created"
    ]
  }
]

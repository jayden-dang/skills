# Runbooks index

Status: Approved
Date: <YYYY-MM-DD>

## Purpose and boundary

<What this document covers. Or: None — <reason>>

## Runbook catalog

| Slug | Path | When to use |
|---|---|---|
| <slug> | `docs/ops/runbooks/<slug>.md` | <when> |

Or: `None — <reason>`

## Authority

Directory entry: **Absent** if `docs/ops/runbooks/` missing; **Approved** when index file
at `docs/ops/runbooks.md` OR catalog entry path is Approved with Status+validator
(pack uses `docs/ops/runbooks.md` as index file for Status header). Individual runbook
files are addressable by slug only (no greppable procedure IDs).


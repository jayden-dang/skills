[
  {
    "eval_id": 1,
    "eval_name": "three-execute-routes-at-exit",
    "kind": "behavior",
    "derived_from": "TESTS.md § Rules — exactly three execute routes at Exit (Exit step 3 table)",
    "prompt": "The design is approved and you have just finished writing tasks.md. Reach the Exit step.",
    "assertions": [
      "exactly three execute routes are presented",
      "the routes are presented as a choice rather than one being started automatically"
    ]
  },
  {
    "eval_id": 2,
    "eval_name": "no-silent-invention-of-execution-mode",
    "kind": "behavior",
    "derived_from": "TESTS.md § Rules — no silent invent of continuous at plan time (Exit step 2 + Red Flags; RED failure 2)",
    "prompt": "Write tasks.md for an approved design. The user has said nothing about how execution should run.",
    "assertions": [
      "no Execution-mode value is written into tasks.md at plan time",
      "continuous mode is not assumed as a default",
      "plan approval is not made to depend on choosing an execution mode first"
    ]
  },
  {
    "eval_id": 3,
    "eval_name": "mode-write-back-belongs-to-the-execute-skill",
    "kind": "behavior",
    "derived_from": "TESTS.md § Rules — mode write-back owned by execute skill",
    "prompt": "The user picks a route at the Exit step.",
    "assertions": [
      "the Execution-mode line is written back by the execute skill rather than by plan-tasks",
      "plan-tasks does not record the mode itself"
    ]
  },
  {
    "eval_id": 4,
    "eval_name": "triad-publishes-one-feature-issue",
    "kind": "behavior",
    "derived_from": "TESTS.md § Rules — triad publish is one feature issue, not one per task (Step 5; S-WP-PUBLISH RED)",
    "prompt": "tasks.md is approved with nine tasks and the repo has a configured tracker. Publish.",
    "assertions": [
      "one feature issue is published, not one issue per task",
      "the issue carries the union of the requirement IDs the tasks cover"
    ]
  },
  {
    "eval_id": 5,
    "eval_name": "trigger-fires-on-task-planning",
    "kind": "trigger",
    "derived_from": "TESTS.md § Description trigger check — should-fire",
    "prompt": "break the design into implementation tasks",
    "assertions": [
      "plan-tasks is selected"
    ]
  },
  {
    "eval_id": 6,
    "eval_name": "trigger-yields-to-design",
    "kind": "trigger",
    "derived_from": "TESTS.md § Description trigger check — should-not-fire",
    "prompt": "design the architecture for this feature",
    "assertions": [
      "design-solution is selected rather than plan-tasks"
    ]
  },
  {
    "eval_id": 7,
    "eval_name": "plan-size-budget-blocks-approve",
    "kind": "behavior",
    "derived_from": "TESTS.md § Rules — task_count > 12 OR line_count > 400 blocks Approve (RED S-PLAN-BUDGET; GREEN v1.2.0)",
    "prompt": "tasks.md has 28 tasks and about 900 lines. User says approve and start building — don't split.",
    "assertions": [
      "Status: Approved is not set while over the plan size budget",
      "task_count and line_count are cited against the ceilings 12 and 400",
      "size remedies offered are decompose, cut scope, or merge slices"
    ]
  },
  {
    "eval_id": 8,
    "eval_name": "recommend-build-by-story-for-ui",
    "kind": "behavior",
    "derived_from": "TESTS.md § Rules — Recommend build-by-story when UI / Solo-Small / ≥2 stories (RED S-PLAN-RECOMMEND; GREEN v1.2.0)",
    "prompt": "Under-budget UI plan with multiple stories on a Small team. User approves and says waves are faster — skip story stops.",
    "assertions": [
      "build-by-story is marked Recommended in the three-route offer",
      "all three execute routes are still offered",
      "Execution-mode is not invented by plan-tasks",
      "waves preference is not treated as a silent route pick"
    ]
  }
]

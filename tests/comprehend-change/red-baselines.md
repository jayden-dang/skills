# XDIFF red baselines (trace-ignored fixtures)

## RED-package
Without skill path / plugin entry: frontmatter and plugin tests fail.

## RED-skill-body
Without cascade prose: pure-untracked falls through to branch packet; no scope notice; invents empty HTML; claims quiz pass.

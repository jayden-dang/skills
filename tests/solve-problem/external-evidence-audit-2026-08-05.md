# solve-problem external evidence — no-op audit

Model roster: `gpt-5.6-sol`, `gpt-5.6-terra`.

Scenario: production Redis `SCAN` reportedly returns duplicates; provider,
distribution, server/client versions, topology, exact command/output, and
applicable owning contract are unknown; user requests replacement before launch.

Both models preserved the gap/class as `unresolved`, separated the replacement
request from the desired outcome, routed the smallest external fact to `research`,
and required reclassification after runtime identity and owning documentation.
Neither classified a Redis defect or started replacement work.

Verdict: current `solve-problem` already covers pre-classification external facts.
No edit is authorized by `author-skills`; the diagnostic external-evidence rule
has one home in `root-cause` after Phase 2.

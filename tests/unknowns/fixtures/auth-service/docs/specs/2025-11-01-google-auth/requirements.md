# Google OAuth login
Status: Shipped

**AUTH-1.1** WHEN a user signs in with Google THE SYSTEM SHALL create a server-side session cookie.

**AUTH-1.2** WHEN a user has an active session THE SYSTEM SHALL NOT issue a JWT access token for the browser client.
